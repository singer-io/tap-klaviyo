coverage:
  status:
    project:
      default:
        target: 80%    # the required coverage value
        threshold: 1%  # the leniency in hitting the target
        paths: tap_klaviyo/*
    patch:
      default:
        target: 90% # New code should have a higher coverage
          threshold: 1%
          paths: tap_klaviyo/*